package io.forgeloop.control.domain; import java.util.Optional; import org.springframework.data.jpa.repository.JpaRepository; public interface FeatureRunRepository extends JpaRepository<FeatureRun,String>{Optional<FeatureRun> findByRepositoryAndSourceRef(String repository,String sourceRef);}
