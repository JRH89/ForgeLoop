server {
  listen 80;
  location / { root /usr/share/nginx/html; try_files $uri $uri/ /index.html; }
  location /graphql { proxy_pass http://control-plane:8090/graphql; proxy_set_header Content-Type $content_type; }
  location /api/github/ { proxy_pass http://control-plane:8090/api/github/; }
}
